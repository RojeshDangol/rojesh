import { AfterViewInit, ElementRef, OnInit, QueryList } from '@angular/core';
import { RaycasterService } from './events/raycaster.service';
import { ThEngineService } from './ThEngine.service';
import { ThView } from './ThView';
import * as i0 from "@angular/core";
export declare class ThCanvas extends ThView implements OnInit, AfterViewInit {
    protected engServ: ThEngineService;
    protected raycaster: RaycasterService;
    private static instanceCnt;
    readonly nid: number;
    /**
     * if true does not use the ThCanvas as view
     * this is esepcially usefull if you have multiple views
     * and want to iterate over all of them with *ngFor
     * use statically (before ngOnInit is triggered)
     */
    disableDefaultView: boolean;
    get obj(): this;
    set views(viewList: QueryList<ThView>);
    protected _rendererCanvas?: ElementRef<HTMLCanvasElement>;
    set rendererCanvas(canvas: ElementRef<HTMLCanvasElement> | undefined);
    get rendererCanvas(): ElementRef<HTMLCanvasElement> | undefined;
    constructor(engServ: ThEngineService, raycaster: RaycasterService);
    ngAfterViewInit(): void;
    ngOnInit(): void;
    static ɵfac: i0.ɵɵFactoryDeclaration<ThCanvas, never>;
    static ɵcmp: i0.ɵɵComponentDeclaration<ThCanvas, "th-canvas", never, { "disableDefaultView": "disableDefaultView"; }, {}, ["views"], never>;
}
